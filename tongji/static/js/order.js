function examine(){
    //获取order.js的orders信息{% for order in orders %}
    var orders = document.getElementById("orders").value;
    //将orders解案戽数组
    var orderList = JSON.parse(orders);
    //获取order.js的order_id信息{% for order in orders %

}